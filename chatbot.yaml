apiVersion: apps/v1
kind: Deployment
metadata:
  name: langflow-chatbot
  namespace: chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: langflow-chatbot
  template:
    metadata:
      labels:
        app: langflow-chatbot
    spec:
      containers:
      - name: langflow-chatbot
        image: gotosatoru/langflow-chatbot:latest
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: chatbot-env
---
apiVersion: v1
kind: Service
metadata:
  name: langflow-chatbot
spec:
  selector:
    app: langflow-chatbot
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
